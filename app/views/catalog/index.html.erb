 <div id="sidebar" class="span3">
    <%= render :partial => 'constraints' %>
    <%= render :partial=>'facets' %>

</div><!--/well -->

<div id="content" class="span9">
  <h2 class="hide-text top-content-title"><%= t('flare.search.search_results_header') %></h2>

  <% @page_title = t('flare.search.title', :application_name => 'Flare') %>

  <% extra_head_content << render_opensearch_response_metadata.html_safe %>

  <%= render :partial => 'did_you_mean' %>
  
  
  <!--
  <ul id="view-tabs" class="nav nav-tabs">
    <li class="active"><a class="btn-view" href="#view-list" data-toggle="tab" data-view="list">List View</a></li>
    <li><a class="btn-view" href="#view-graph" data-toggle="tab" data-view="graph">Graph View</a></li>
  </ul>
  -->
  <div class="tab-content">
    <div class="tab-pane fade in active" id="view-list">
      <%= render 'sort_and_per_page' %>

      <div id="resetSearch" class="btn-group" style="margin-top:-5px; float:right;">
        <a  class="btn" href= "/search/">Reset my search</a>
      </div>

      <h2 class="hide-text"><%= t('flare.search.search_results') %></h2>
      
      <%= render_document_index %>
    
      <%= render 'results_pagination' %>
    </div>
    <div class="tab-pane fade in" id="view-graph">
      <%= render :partial => 'results_header' %>
      
      <%= render :partial => 'graph' %>
    </div>
  </div>
</div>
