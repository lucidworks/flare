    <ul class="bookmarkTools">
      <li class="email">
        <%= link_to t('blacklight.tools.email'), email_catalog_path(:collection_id => params[:collection_id], :sort=>params[:sort], :per_page=>params[:per_page], :id => @bookmarks.collect {|doc| doc.document_id}), :class=>"lightboxLink btn", :id => "emailLink" %>
      </li>
      <li>
        <%= link_to t('blacklight.bookmarks.clear.action_title'), clear_bookmarks_path(:collection_id => params[:collection_id]), :method => :delete, :data => { :confirm => t('blacklight.bookmarks.clear.action_confirm') }, :class => 'btn' %>
      </li>
</ul>
